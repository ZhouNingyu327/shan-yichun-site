---
import { NAV_ITEMS } from '@/utils/constants';
---

<div class="md:hidden">
  <button id="menu-toggle" class="p-2 text-white/65 hover:text-white transition-colors" aria-label="打开菜单">
    <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
      <path d="M4 6h16M4 12h16M4 18h16" />
    </svg>
  </button>

  <div id="mobile-menu" class="hidden absolute top-16 left-0 right-0 border-b border-white/10 bg-[#121020] shadow-2xl">
    <div class="px-4 py-3 space-y-1">
      {
        NAV_ITEMS.map((item) => (
          <a
            href={item.href}
            class="block px-3 py-2.5 text-base font-medium text-white/65 hover:text-white hover:bg-white/[0.07] rounded-lg transition-colors"
          >
            {item.label}
          </a>
        ))
      }
    </div>
  </div>
</div>

<script>
  const toggle = document.getElementById('menu-toggle');
  const menu = document.getElementById('mobile-menu');
  if (toggle && menu) {
    toggle.addEventListener('click', () => {
      menu.classList.toggle('hidden');
    });
  }
</script>
